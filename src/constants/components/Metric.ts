import { MetricProps } from '../../@types';

export const metricsVariants: Record<Required<MetricProps>['variant'], string> =
  {
    material: 'Material',
    size: 'Tamanho',
    weight: 'Peso',
  };
